import { Box, styled } from "@mui/material";

export const FullScreenWrapper = styled(Box)({
  alignItems: "center",
  display: "flex",
  height: "100vh",
  justifyContent: "center",
  width: "100vh",
});
